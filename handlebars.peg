
expression <- "{{" < handlebars* > "}}" {
            hb_event(yy->handlebars, EVENT_EXPRESSION, yytext); }
        / < not_handlebars+ > { hb_event(yy->handlebars, EVENT_TEXT, yytext); }
not_handlebars <- !"{{" .
handlebars <- !"}}" .
