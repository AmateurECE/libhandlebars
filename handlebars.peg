
document <- expression+
expression <- < [^\\] > starttoken { printf("START: %c\n", yytext); }
        / endtoken { printf("END\n"); }
        / < text > { printf("TT: %s\n", yytext); }
starttoken <- "{{"
endtoken <- "}}"
text <- token
token <- .